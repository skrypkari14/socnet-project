import React from 'react';

const Select = ({className}) => {
    return (
        <div className={`${className}`}>
            <button>

            </button>
        </div>
    );
};

export default Select;